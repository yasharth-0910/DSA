class Solution {
public:
    int shipWithinDays(vector<int>& weights, int days) {
        int i =1;
        int j = INT_MAX;

        while( i<=j){
            int mid = i + (j-i)/2;
            
            
        }
    }
};